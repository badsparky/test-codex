# Flask Othello Game

A minimal Othello (Reversi) game implemented with Flask.

## Setup

Install dependencies and run the app:

```bash
pip install -r requirements.txt
python app.py
```

Access `http://localhost:5000` in your browser to play.
